---
type: llm
weight: 2
---

"Testing agents" is a broad, ambiguous topic in this corpus that spans multiple distinct
concept pages (things like eval harness design, LLM-as-a-judge, verifier design,
trajectory evaluation, benchmark contamination/reward hacking, coding agent
benchmarking). A good answer synthesizes across several of these rather than treating it
as a single narrow concept.

Pass only if the response draws on at least two distinct underlying themes/concepts (not
just one page's content) and grounds claims in named talks/speakers with citations.

Fail if the response is grounded in only a single concept page's content when the
question clearly spans more, or if it answers from general knowledge about agent testing
rather than the corpus.
